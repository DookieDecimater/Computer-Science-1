#sort a list
#jackson bauer
list = [1,99,5,32,8,59,22,22,43,7,6,11]
input = 'do you want to sort by (asc) or (desc)'
def lnum(lnum):
    if input.lower == asc:
        list.sort(reverse=True)
        print(list)
    elif input.lower == desc:
        lsit.sort(reverse=False)
        print(list)

lnum(list)
